import { Box, Container } from '@mui/material';
import React from 'react';

const HeroSection = () => {
    return (
        <Box>
            <Container>
                Hero Section
            </Container>
        </Box>
    );
};

export default HeroSection;